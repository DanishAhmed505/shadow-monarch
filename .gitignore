# build output (regenerated by `python build.py`; CI builds it on deploy)
/public/

# python
__pycache__/
*.pyc
.venv/
venv/

# preserved dynamic version's local database
flask-version/blog.db

# os
.DS_Store

# node (wrangler CLI, used only to deploy the claps Worker)
node_modules/
.wrangler/
